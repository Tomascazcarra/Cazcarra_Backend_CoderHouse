<div> 
    {{#if productos}}
    <h1>
        Lista de productos:
    </h1>
    <ul>
        {{#each productos}}
        <li>
            <p>{{this.name}} {{this.quantity}}</p>
        </li>
        {{/each}}
    </ul>
    {{else}}
    <h1>Aun no hay productos en la lista</h1>
    {{/if}}
</div>